clear; clc;

PID = input('New patient ID: ','s');
cd ../../patients

if ~exist(PID, 'dir')
    mkdir(PID);
    cd(PID)
    mkdir('Binary Data');
    mkdir('cal');
    mkdir('Calibrated Data');
    mkdir('Uncalibrated Data');
else
    fprintf('This 
end
